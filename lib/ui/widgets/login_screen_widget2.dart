import 'package:flutter/material.dart';

class LoginScreenWidget2 extends StatelessWidget {
  const LoginScreenWidget2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
